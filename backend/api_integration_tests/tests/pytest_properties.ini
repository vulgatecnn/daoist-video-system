[tool:pytest]
testpaths = .
python_files = test_*_properties.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --disable-warnings
    --no-migrations
    --reuse-db
markers =
    property: Property-based tests
    unit: Unit tests
    integration: Integration tests